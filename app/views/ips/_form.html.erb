<%= form_for(@ip) do |f| %>
  <% if @ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip.errors.count, "error") %> prohibited this ip from being saved:</h2>

      <ul>
      <% @ip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip %><br>
    <%= f.text_field :ip %>
  </div>
  <div class="field">
    <%= f.label :class_c %><br>
    <%= f.text_field :class_c %>
  </div>
  <div class="field">
    <%= f.label :occupado %><br>
    <%= f.check_box :occupado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
